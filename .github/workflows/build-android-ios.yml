name: Build ChutaXuta Android + iOS Sideload

on:
  workflow_dispatch:

jobs:
  android:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'
      - name: Build APK
        shell: bash
        run: |
          set -euo pipefail
          cd ChulaXuta_Android
          gradle --no-daemon assembleDebug
          mkdir -p ../dist-android
          APK="$(find app/build/outputs/apk/debug -type f -name '*.apk' -print -quit)"
          test -n "$APK"
          cp "$APK" ../dist-android/ChutaXuta-v1.45.apk
      - uses: actions/upload-artifact@v4
        with:
          name: ChutaXuta-v1.45-Android-APK
          path: dist-android/ChutaXuta-v1.45.apk
          if-no-files-found: error

  ios-sideload:
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Build unsigned IPA
        shell: bash
        run: |
          set -euo pipefail
          cd ChutaXuta_iOS
          xcodebuild \
            -project ChutaXuta.xcodeproj \
            -scheme ChutaXuta \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            build
          APP="$(find build/Build/Products/Release-iphoneos -maxdepth 1 -type d -name '*.app' -print -quit)"
          test -n "$APP"
          rm -rf Payload
          mkdir Payload
          cp -R "$APP" Payload/ChutaXuta.app
          zip -qry ChutaXuta-v1.45-unsigned.ipa Payload
      - uses: actions/upload-artifact@v4
        with:
          name: ChutaXuta-v1.45-iOS-Sideload-IPA
          path: ChutaXuta_iOS/ChutaXuta-v1.45-unsigned.ipa
          if-no-files-found: error
